const resetHostsStub = `# localhost name resolution is handled within DNS itself.
# do not change anything in here if you don't know what you are doing.

127.0.0.1	localhost
::1		localhost
`;

module.exports = resetHostsStub;
